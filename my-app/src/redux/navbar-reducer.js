let initialState = {
        friends: [
            {id: 1, userName: "User1"},
            {id: 2, userName: "User2"},
            {id: 3, userName: "User3"},
        ],
}

const navbarReducer = (state = initialState, action) => {
    return state;
}

export default navbarReducer;